/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: the integer number to compute its absolute value.
 * Return: n if n is negative other wise -1*n
 */
int _abs(int n)
{
	return (n >= 0 ? n : -1 * n);
}

